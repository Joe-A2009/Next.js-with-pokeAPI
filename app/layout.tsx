import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ejemplos de uso de rutas dinámicas",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased  bg-red-500 text-white `}
      >
        {children}
      </body>
    </html>
  );
}
//al obtener children definimos un lang y en el body definimos en el classname el tipo de letra, 
//luego en {children} estamos diciendo que mandamos todo el contenido del page.tsx que esta en children
//al body, dandonos ese formato base del layout